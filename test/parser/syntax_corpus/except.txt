=============|||
Basic record update
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT ![2] = "a"]
====
---- MODULE TestPrelude ----
VARIABLE a
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (nat_number)))
              new_val: (string))
        ))
(double_line)))

=============|||
Record Update with Multiple Parameters
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [f EXCEPT ![1, 1] = 0]
====
---- MODULE TestPrelude ----
VARIABLE f
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (tuple_literal (langle_bracket)
                      (nat_number) (nat_number)
                    (rangle_bracket))))
              new_val: (nat_number))
        ))
(double_line)))

=============|||
Record Update with Multi-Value Tuple Parameter
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [f EXCEPT ![<<1, 1>>] = 0]
====
---- MODULE TestPrelude ----
VARIABLE f
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (tuple_literal (langle_bracket)
                      (nat_number) (nat_number)
                    (rangle_bracket))))
              new_val: (nat_number))
        ))
(double_line)))

=============|||
Record Update with Single-Value Tuple Parameter
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [f EXCEPT ![<<1>>] = 0]
====
---- MODULE TestPrelude ----
VARIABLE f
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (tuple_literal (langle_bracket)
                      (nat_number)
                    (rangle_bracket))))
              new_val: (nat_number))
        ))
(double_line)))

=============|||
Record Update with Tuple and Non-Tuple Parameters
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [f EXCEPT ![<<1, 1>>, 1] = 0]
====
---- MODULE TestPrelude ----
VARIABLE f
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl
                      (tuple_literal (langle_bracket)
                        (tuple_literal (langle_bracket) (nat_number) (nat_number) (rangle_bracket))
                        (nat_number)
                      (rangle_bracket))))
              new_val: (nat_number))
        ))
(double_line)))

=============|||
Single nested record update
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT ![2]["a"][3] = @ + 1]
====
---- MODULE TestPrelude ----
VARIABLE a
CONSTANT _ + _
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition: (except
      expr_to_update: (identifier_ref)
      (except_update
        update_specifier: (except_update_specifier
          (except_update_fn_appl (nat_number))
          (except_update_fn_appl (string))
          (except_update_fn_appl (nat_number))
        )
        new_val: (bound_infix_op lhs: (prev_func_val) symbol: (plus) rhs: (nat_number))
      )
    )
  )
(double_line)))

=============|||
Single nested record update with dot syntax
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT !.x.y.z = "b"]
====
---- MODULE TestPrelude ----
VARIABLE a
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref)))
              new_val: (string))
        ))
(double_line)))

=============|||
Multiple nested record updates
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT ![2]["a"][3] = @, !["b"][x] = 5, ![y][z] = "c"]
====
---- MODULE TestPrelude ----
VARIABLES a, x, y, z
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (nat_number))
                    (except_update_fn_appl (string))
                    (except_update_fn_appl (nat_number)))
              new_val: (prev_func_val))
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (string))
                    (except_update_fn_appl (identifier_ref)))
              new_val: (nat_number))
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (identifier_ref))
                    (except_update_fn_appl (identifier_ref)))
              new_val: (string))
        ))
(double_line)))

=============|||
Multiple nested record updates with dot syntax
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT !.a.b.c = @, !.b.x = 5]
====
---- MODULE TestPrelude ----
VARIABLE a
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref)))
              new_val: (prev_func_val))
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref)))
              new_val: (nat_number))
        ))
(double_line)))

=============|||
Multiple nested record updates with mixed dot/function syntax
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
r == [a EXCEPT !.a.b["c"] = "b", ![2].x = @]
====
---- MODULE TestPrelude ----
VARIABLE a
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq)
    definition:
        (except
            expr_to_update: (identifier_ref)
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_record_field (identifier_ref))
                    (except_update_record_field (identifier_ref))
                    (except_update_fn_appl (string)))
              new_val: (string))
            (except_update
              update_specifier:
                (except_update_specifier
                    (except_update_fn_appl (nat_number))
                    (except_update_record_field (identifier_ref)))
              new_val: (prev_func_val))
        ))
(double_line)))

