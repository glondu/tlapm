=============|||
RECURSIVE inside LET/IN
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
op ==
  LET
    RECURSIVE f(_)
    f(n) == IF n =< 0 THEN 1 ELSE n * f(n-1)
  IN f(5)
====
---- MODULE TestPrelude ----
x =< y == TRUE
x * y == 0
x - y == 0
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq) definition:
    (let_in
      (recursive_declaration (operator_declaration (identifier) (placeholder)))
      definitions:
        (operator_definition name: (identifier) parameter: (identifier) (def_eq) definition:
          (if_then_else
            if: (bound_infix_op lhs: (identifier_ref) symbol: (leq) rhs: (nat_number))
            then: (nat_number)
            else: (bound_infix_op lhs: (identifier_ref) symbol: (mul) rhs:
              (bound_op name: (identifier_ref) parameter:
                (bound_infix_op lhs: (identifier_ref) symbol: (minus) rhs: (nat_number))
              )
            )
          )
        )
      expression:
        (bound_op name: (identifier_ref) parameter: (nat_number))
    )
  )
(double_line)))

=============|||
Conjlist with LET/IN
=============|||

---- MODULE Test ----
op ==
  /\ LET x == 1 IN 2
  /\ 3
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (operator_definition name: (identifier) (def_eq) definition:
    (conj_list
      (conj_item
        (bullet_conj)
        (let_in
          definitions: (operator_definition name: (identifier) (def_eq) definition: (nat_number))
          expression: (nat_number)
        )
      )
      (conj_item (bullet_conj) (nat_number))
    )
  )
(double_line)))

=============|||
Conjlist with RECURSIVE in LET/IN
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
op ==
  /\ LET
       RECURSIVE f
       f == f
     IN P
  /\ Q
====
---- MODULE TestPrelude ----
P == TRUE
Q == TRUE
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq) definition:
    (conj_list
      (conj_item
        (bullet_conj)
        (let_in
          definitions:
            (recursive_declaration (identifier))
            (operator_definition name: (identifier) (def_eq) definition:
              (identifier_ref))
          expression: (identifier_ref)
        )
      )
      (conj_item (bullet_conj) (identifier_ref))
    )
  )
(double_line)))

=============|||
Conjlist with INSTANCE in LET/IN
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
op ==
  /\ LET M1 == INSTANCE M2 IN P
  /\ Q
====
---- MODULE M2 ----
====
---- MODULE TestPrelude ----
P == TRUE
Q == TRUE
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq) definition:
    (conj_list
      (conj_item
        (bullet_conj)
        (let_in
          definitions: (module_definition
            name: (identifier) (def_eq)
            definition: (instance (identifier_ref)))
          expression: (identifier_ref)
        )
      )
      (conj_item (bullet_conj) (identifier_ref))
    )
  )
(double_line)))

=============|||
Conjlist inside LET/IN
=============|||

---- MODULE Test ----
op ==
  LET
    x ==
      /\ 1
      /\ 2
        IN 3
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (operator_definition name: (identifier) (def_eq) definition:
    (let_in
      definitions: (operator_definition name: (identifier) (def_eq) definition:
        (conj_list
          (conj_item (bullet_conj) (nat_number))
          (conj_item (bullet_conj) (nat_number))
        )
      )
      expression: (nat_number)
    )
  )
(double_line)))

=============|||
Disjlist with LET/IN
=============|||

---- MODULE Test ----
op ==
  \/ LET x == 1 IN 2
  \/ 3
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (operator_definition name: (identifier) (def_eq) definition:
    (disj_list
      (disj_item
        (bullet_disj)
        (let_in
          definitions: (operator_definition
            name: (identifier) (def_eq)
            definition: (nat_number))
          expression: (nat_number)
        )
      )
      (disj_item (bullet_disj) (nat_number))
    )
  )
(double_line)))

=============|||
Disjlist with RECURSIVE in LET/IN
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
op ==
  \/ LET
       RECURSIVE f
       f == f
     IN P
  \/ Q
====
---- MODULE TestPrelude ----
P == TRUE
Q == TRUE
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq) definition:
    (disj_list
      (disj_item
        (bullet_disj)
        (let_in definitions:
          (recursive_declaration (identifier))
          (operator_definition name: (identifier) (def_eq) definition: (identifier_ref))
          expression:
          (identifier_ref)
        )
      )
      (disj_item (bullet_disj) (identifier_ref))
    )
  )
(double_line)))

=============|||
Disjlist with INSTANCE in LET/IN
=============|||

---- MODULE Test ----
EXTENDS TestPrelude
op ==
  \/ LET M1 == INSTANCE M2 IN P
  \/ Q
====
---- MODULE M2 ----
====
---- MODULE TestPrelude ----
P == TRUE
Q == TRUE
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (extends (identifier_ref))
  (operator_definition name: (identifier) (def_eq) definition:
    (disj_list
      (disj_item
        (bullet_disj)
        (let_in
          definitions: (module_definition
            name: (identifier) (def_eq)
            definition: (instance (identifier_ref)))
          expression: (identifier_ref)
        )
      )
      (disj_item (bullet_disj) (identifier_ref))
    )
  )
(double_line)))

=============|||
Disjlist inside LET/IN
=============|||

---- MODULE Test ----
op ==
  LET
    x ==
      \/ 1
      \/ 2
        IN 3
====

-------------|||

(source_file (module (header_line) name: (identifier) (header_line)
  (operator_definition name: (identifier) (def_eq) definition:
    (let_in
      definitions: (operator_definition
        name: (identifier) (def_eq)
        definition: (disj_list
          (disj_item (bullet_disj) (nat_number))
          (disj_item (bullet_disj) (nat_number))
        )
      )
      expression: (nat_number)
    )
  )
(double_line)))

=============|||
Invalid Use of LOCAL in LET/IN
:error
=============|||

---- MODULE LocalInLetIn ----
foo == LET LOCAL bar == 5 IN TRUE
====

-------------|||

